# Sinatra / Omniauth sample app 

## Running locally

You will need:

* Ruby 2.7.5

### Instructions

* Checkout the repository
* Run `bundle install`
* Run `rake up`
* Browse to http://localhost:8080
